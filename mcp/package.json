{
  "name": "nl-library-via-mcp",
  "version": "0.1.0",
  "description": "국립중앙도서관 자동 로그인 (samu-browser MCP 경유)",
  "type": "module",
  "bin": {
    "library-via-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.2"
  }
}
